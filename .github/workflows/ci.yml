# CI workflow — disabled to avoid GitHub Actions free tier usage.
# To re-enable: remove the `# ` prefix from each line.
#
# NOTE: Vitest 4.x requires Node 22+ (uses `styleText` from `node:util`).
# If re-enabling, use node-version: [22] only, and run `npm install` first
# to generate package-lock.json before committing.
#
# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         node-version: [22]
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: ${{ matrix.node-version }}
#       - run: npm install
#       - run: npm run typecheck
#       - run: npm test
